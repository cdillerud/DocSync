name: Apply Document Link Recovery Endpoint Patch

on:
  push:
    branches:
      - feature/square9-parity-systemid-gate
    paths:
      - '.github/workflows/apply-document-link-recovery-endpoint-patch.yml'
      - 'backend/scripts/apply_document_link_recovery_endpoint_patch.py'

permissions:
  contents: write

jobs:
  apply:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: feature/square9-parity-systemid-gate
          fetch-depth: 0

      - name: Apply exact recovery endpoint patch
        run: python backend/scripts/apply_document_link_recovery_endpoint_patch.py

      - name: Verify only intended production file changed
        run: |
          git diff --exit-code -- .github/workflows/apply-document-link-recovery-endpoint-patch.yml backend/scripts/apply_document_link_recovery_endpoint_patch.py
          changed="$(git diff --name-only)"
          test "$changed" = "backend/routers/gpi_integration.py" || {
            echo "::error::Unexpected changed files: $changed"
            exit 1
          }

      - name: Commit recovery endpoint
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add -- backend/routers/gpi_integration.py
          git commit -m "parity: add duplicate-free BC link recovery"
          git push origin HEAD:feature/square9-parity-systemid-gate
