name: Apply Historical Migration Dedupe Patch

on:
  push:
    branches:
      - feature/square9-parity-systemid-gate
    paths:
      - '.github/workflows/apply-migration-dedupe-patch.yml'
      - 'backend/scripts/apply_migration_dedupe_patch.py'

permissions:
  contents: write

jobs:
  apply:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: feature/square9-parity-systemid-gate
          fetch-depth: 0

      - name: Apply exact migration dedupe patch
        run: python backend/scripts/apply_migration_dedupe_patch.py

      - name: Verify only intended production file changed
        run: |
          git diff --exit-code -- .github/workflows/apply-migration-dedupe-patch.yml backend/scripts/apply_migration_dedupe_patch.py
          changed="$(git diff --name-only)"
          test "$changed" = "backend/services/migration/job.py" || {
            echo "::error::Unexpected changed files: $changed"
            exit 1
          }

      - name: Commit source-scoped migration dedupe
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add -- backend/services/migration/job.py
          git commit -m "parity: make historical migration dedupe source-scoped"
          git push origin HEAD:feature/square9-parity-systemid-gate
