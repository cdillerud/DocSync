"""Contract-intelligence services (Phase 1 placeholder).

Phase 2 will populate this package with `agreement_normalizer.py` and
`bc_agreement_matcher.py`. Phase 1 ships only the empty package marker
so import paths are reservable and visible in code search.
"""
