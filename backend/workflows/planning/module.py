"""Planning / Import module (Lane C Step 8).

Scope: pure deterministic foundation for Coloplast-style forecast
imports. No LLM. No persistence. No routes. No scheduler. No live
consumers in this step.

Runtime behavior in Step 8: ZERO. The package is importable from
tests; no production code imports it.
"""
