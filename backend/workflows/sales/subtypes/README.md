# Sales Subtypes

Empty placeholder directories per `LANE_A_SIGNED_SCOPE.md` §2.1.

`services/so_rules_engine.py` remains in `backend/services/` until Lane C step 6; subtype directories here are scaffold-only and intentionally contain no `__init__.py`.
